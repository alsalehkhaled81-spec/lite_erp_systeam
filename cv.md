# تقرير: استخدام الذكاء الاصطناعي في تقييم السير الذاتية

هذا التقرير يوثّق كيف يعمل نظام تقييم السير الذاتية (CVs) بالذكاء الاصطناعي ضمن نظام ERP — البيانات المُدخَلة، آلية المعالجة، مخرجات التقييم، وحدود النظام الحالية.

---

## الملفات الأساسية المعنية

| الملف | الدور |
|------|-------|
| `app/Filament/Hr/Resources/ResumeResource.php` | المورد الرئيسي: يعرض جدول السير الذاتية ويعرّف زر "تحليل بالذكاء الاصطناعي". |
| `app/Services/ResumeAnalysisService.php` | الخدمة الأساسية: تبني الـ Prompt، تستدعي الذكاء الاصطناعي، وتُرجع النتيجة بصيغة JSON منظّمة. |
| `app/Services/AiService.php` | خدمة الاتصال العامة بالذكاء الاصطناعي عبر وسيط LiteLLM (HTTP POST). |
| `app/Models/Resume.php` | النموذج: يحوي `employee_id`، `file_path`، `resume_text`. |
| `database/migrations/2026_04_04_104707_create_resumes_table.php` | مخطط جدول `resumes`. |

---

## 1. مخطط البيانات (Database Schema)

جدول `resumes` بسيط ومرتبط بالمُوظّف:

```
resumes
├── id              (المعرف)
├── employee_id     (FK → employees، فريد — لكل موظف سيرة واحدة)
├── file_path       (مسار ملف PDF/Word المرفوع، nullable)
├── resume_text     (النص المستخرج من الملف، longText، nullable)  ← يقرأه الذكاء الاصطناعي
└── timestamps
```

> **ملاحظة مهمة:** النظام يعتمد على حقل `resume_text` كنص خام يُغذّى للذكاء الاصطناعي. حالياً لا يوجد استخراج آلي للنص من الملف المرفوع — يجب إدخال النص يدوياً أو مستقبلاً عبر API استخراج النص (انظر `JobApplicationController.php:54`).

---

## 2. نقطة الدخول (كيف يبدأ التحليل)

ضمن صفحة السير الذاتية في لوحة الموارد البشرية (HR) يوجد زر إجراء على كل سجل:

```php
// app/Filament/Hr/Resources/ResumeResource.php
Tables\Actions\Action::make('analyze_resume')
    ->label(__('filament.actions.ai_analyze_resume'))
    ->icon('heroicon-o-sparkles')
    ->color('warning')
    ->form([
        // يطلب من المستخدم إدخال معيارين قبل التحليل:
        Forms\Components\TextInput::make('target_job_title')   // المسمى الوظيفي المستهدف
            ->required(),
        Forms\Components\Textarea::make('job_keywords')        // الكلمات المفتاحية المطلوبة
            ->required(),
    ])
```

عند الضغط يظهر مودال يطلب من مسؤول الموارد البشرية:
1. **المسمى الوظيفي المستهدف** — الوظيفة المراد التقييم على أساسها (مثال: مطور PHP).
2. **الكلمات المفتاحية المطلوبة** — المهارات/المعايير المرجعية للمقارنة.

---

## 3. المدخلات: البيانات المُجمَّعة للتحليل

عند تنفيذ الإجراء (`ResumeResource.php:117`) تُجمَع البيانات التالية من الموظف المرتبط بالسيرة:

```php
$employee = $record->employee;
$employee->load(['user', 'department', 'skills']);

$resumeData = [
    'employee_name' => $employee->user?->name,           // الاسم
    'job_title'     => $employee->job_title,             // المسمى الوظيفي الحالي
    'department'    => $employee->department?->name,     // القسم
    'salary'        => $employee->salary,                // الراتب
    'status'        => $employee->status,                // الحالة الوظيفية
    'skills'        => $skillsList,                      // المهارات المسجلة (قائمة مفصولة بفواصل)
    'resume_text'   => $record->resume_text,             // نص السيرة الذاتية الكامل
];
```

بالإضافة إلى المُدخلات التي يقدّمها المستخدم:
- `job_keywords` — الكلمات المفتاحية المطلوبة للوظيفة.
- `target_job_title` — المسمى الوظيفي المستهدف.

### مصادر البيانات

| الحقل | المصدر |
|-------|--------|
| `employee_name` | جدول `users` (علاقة `employee.user`) |
| `job_title`, `salary`, `status` | جدول `employees` |
| `department` | جدول `departments` (علاقة `employee.department`) |
| `skills` | جدول `skills` عبر علاقة many-to-many (`employee.skills`) |
| `resume_text` | حقل النص المستخرج في جدول `resumes` |

---

## 4. آلية المعالجة: بناء الـ Prompt

دالة `analyzeResume()` في `ResumeAnalysisService.php:9` تبني محادثة من رسالتين:

### رسالة النظام (System Message)

توجّه النموذج لدور خبير توظيف وتفرض صيغة الإجابة:

```
أنت خبير في الموارد البشرية وتحليل السير الذاتية.
مهمتك هي تحليل السيرة الذاتية للمتقدم ومطابقتها مع متطلبات
الوظيفة المستهدفة.
أجب دائماً باللغة العربية.
أجب بصيغة JSON فقط بدون أي نص إضافي.
```

### رسالة المستخدم (User Message)

تحوي كل البيانات ومعايير التقييم وتطلب إخراجاً منظّماً:

```
قم بتحليل السيرة الذاتية التالية وتقييم مدى مطابقة المتقدم...

## بيانات المتقدم: (الاسم، المسمى المستهدف، المسمى الحالي، القسم، الحالة، الراتب)
## المهارات المسجلة: {...}
## نص السيرة الذاتية: {...}
## الكلمات المفتاحية المطلوبة للوظيفة: {...}

المطلوب:
1. تقييم من 100 (score)
2. تقرير مفصل يوضح أسباب التقييم (report)
3. نقاط القوة (strengths) - قائمة
4. نقاط الضعف (weaknesses) - قائمة
5. التوصية النهائية (recommendation): مقبول / مشروط / مرفوض
6. ملخص تنفيذي (summary): فقرة واحدة

أجب بصيغة JSON التالية فقط:
{ "score": 85, "report": "...", "strengths": [...], ... }
```

---

## 5. الاتصال بالذكاء الاصطناعي

عبر `AiService::chat()` (`AiService.php:16`) يُرسَل طلب HTTP POST إلى وسيط **LiteLLM**:

```php
$response = Http::withHeaders([
    'x-litellm-api-key' => config('ai.api_key'),
    'Content-Type'      => 'application/json',
])->timeout(120)->post(config('ai.api_url'), [
    'model'       => config('ai.model'),
    'messages'    => $messages,
    'max_tokens'  => config('ai.max_tokens', 4096),
    'temperature' => config('ai.temperature', 0.7),
]);
```

> **ملاحظة الأداء:** `set_time_limit(180)` في بداية `analyzeResume()` (`ResumeAnalysisService.php:11`) لرفع حد زمن التنفيذ إلى 3 دقائق، نظراً لطول زمن توليد التحليلات المفصّلة.

---

## 6. معالجة الاستجابة (Parsing)

النموذج مُلزَم بالردّ بصيغة JSON. تتم المعالجة كالتالي:

### أ) تنظيف النص

```php
$content = preg_replace('/```json\s*|\s*```/', '', $content); // إزالة أغلفة markdown
$content = trim($content);
```

### ب) فك تشفير JSON

```php
$decoded = json_decode($content, true);
```

### ج) التعامل مع الفشل

إذا تعذّر فك JSON (استجابة غير صالحة)، يُرجَع كائن احتياطي بدل رمي خطأ:

```php
if (json_last_error() !== JSON_ERROR_NONE) {
    return [
        'score' => 0,
        'report' => $content,                      // النص الخام كتقرير بديل
        'strengths' => [],
        'weaknesses' => [],
        'recommendation' => 'خطأ في التحليل',
        'summary' => 'لم يتمكن الذكاء الاصطناعي من تحليل السيرة الذاتية بشكل صحيح.',
        'raw_response' => $content,
    ];
}
```

وإذا فشل الاتصال بالكامل (`$response === null`)، تُرجع الدالة `null` ويعرض النظام إشعار خطأ.

---

## 7. المخرجات: نتيجة التقييم

بنية JSON المتوقّعة من الذكاء الاصطناعي:

| الحقل | النوع | الوصف |
|-------|------|-------|
| `score` | رقم | درجة المطابقة من 100 |
| `report` | نص | تقرير مفصل يبرّر التقييم |
| `strengths` | مصفوفة | قائمة بنقاط القوة |
| `weaknesses` | مصفوفة | قائمة بنقاط الضعف |
| `recommendation` | نص | التوصية: مقبول / مشروط / مرفوض |
| `summary` | نص | ملخص تنفيذي بفقرة واحدة |

---

## 8. عرض النتيجة للمستخدم

على عكس تقييم الموظف، النتيجة هنا **لا تُحفَظ** في قاعدة البيانات — بل تُعرَض كإشعار (Notification) دائم:

```php
// ResumeResource.php:146-183
$color = $score >= 70 ? 'success' : ($score >= 40 ? 'warning' : 'danger');

Notification::make()
    ->title(__('filament.notifications.ai_analysis_complete') . " ({$score}/100)")
    ->body(new HtmlString(Str::markdown(implode("\n", $reportLines))))
    ->{$color}()        // لون الإشعار حسب الدرجة
    ->persistent()      // إشعار دائم لا يختفي تلقائياً
    ->send();
```

الإشعار يعرض:
- **الدرجة** (مثل `85/100`) في العنوان.
- **التوصية النهائية** (مقبول/مشروط/مرفوض).
- **الملخص التنفيذي**.
- **التقرير المفصل**.
- **نقاط القوة والضعف** كقوائم نقطية.
- **لون الإشعار** أخضر (≥70)، أصفر (40–69)، أحمر (<40).

---

## 9. مخطط التدفق الكامل

```
صفحة السير الذاتية (ResumeResource - لوحة HR)
        │
        ▼
زر "تحليل بالذكاء الاصطناعي" → مودال يطلب:
        ├── المسمى الوظيفي المستهدف (target_job_title)
        └── الكلمات المفتاحية المطلوبة (job_keywords)
        │
        ▼
جمع بيانات الموظف + نص السيرة (ResumeResource::action)
        │
        ▼
ResumeAnalysisService::analyzeResume()
        │
        ├──► بناء الرسائل (system + user prompt)
        │
        ├──► AiService::chat() → HTTP POST إلى LiteLLM
        │        └── model: config('ai.model')
        │
        ├──► تنظيف الاستجابة (إزالة ```json```)
        ├──► json_decode → مصفوفة
        │
        └──► إرجاع: { score, report, strengths, weaknesses, recommendation, summary }
                │
                ▼
        عرض كإشعار (Notification) دائم بلون حسب الدرجة
        (لا يُحفظ في قاعدة البيانات)
```

---

## 10. المقارنة مع تقييم الموظف

| المعيار | تقييم السيرة الذاتية | تقييم الموظف (الأداء) |
|--------|---------------------|----------------------|
| **الغرض** | فرز المتقدمين للتوظيف | تقييم أداء موظف حالي |
| **الأساس** | مطابقة المهارات/النص مع متطلبات وظيفة | إحصائيات أداء (مهام/إجازات/تقارير) |
| **صيغة الإخراج** | JSON منظّم | نص Markdown حر |
| **التدخل البشري** | يطلب مسمى وظيفي + كلمات مفتاحية | تلقائي بالكامل من البيانات |
| **الدرجة** | رقمية (0–100) | نصية (ممتاز/جيد/ضعيف) |
| **الحفظ** | لا يُحفَظ (إشعار فقط) | لا يُحفَظ (مودال فقط) |

---

## 11. الحدود والقيود الحالية

1. **لا يوجد استخراج آلي للنص:** حقل `resume_text` يجب تعبئته يدوياً. رفع ملف PDF/Word لا يستخرج نصه تلقائياً (توجد تعليقة مستقبلية في `JobApplicationController.php:54`). سيرة بنص فارغ ستُحلَّل بجودة منخفضة.

2. **عدم حفظ النتائج:** نتيجة التحليل تظهر في إشعار مؤقت ولا تُخزَّن — إعادة التحليل مطلوبة في كل مرة، وهذا يستهلك استدعاءات إضافية للنموذج.

3. **الاعتماد على جودة الكلمات المفتاحية:** دقة المطابقة تتأثر مباشرة بمدى دقة الكلمات المفتاحية التي يُدخلها مسؤول الموارد البشرية.

4. **عشوائية النتائج:** بسبب `temperature = 0.7`، قد تختلف الدرجة قليلاً بين تحليلين لنفس السيرة.

5. **لا يوجد حد أقصى لحجم النص:** سير ذاتية طويلة جداً قد تتجاوز حد سياق النموذج (context window) دون تقسيم.

6. **معالجة أخطاء JSON:** عند فشل فك JSON، يُعرض النص الخام بدلاً من نتيجة منظّمة، مما يقلل من قابلية القراءة.

7. **مدة التنفيذ:** رفع `set_time_limit` إلى 180 ثانية قد يسبب تجاوز المهلة (timeout) في بعض بيئات الاستضافة.

---

## 12. توصيات للتحسين المستقبلي

- **استخراج النص آلياً:** دمج مكتبة (مثل `smalot/pdfparser` أو `spatie/pdf-to-text`) لاستخراج نص PDF تلقائياً عند الرفع وحفظه في `resume_text`.
- **حفظ نتائج التحليل:** إضافة أعمدة `ai_score`, `ai_summary`, `ai_report`, `analyzed_at` لجدول `resumes` لتخزين آخر تقييم.
- **معالجة الدفعات (Batch):** تحليل عدة سير دفعة واحدة مقابل نفس الوظيفة لترتيب المرشحين.
- **تخفيض temperature** إلى `0.2` أو `0.3` لنتائج أكثر اتساقاً في التقييم الكمي.
- **تقسيم النصوص الطويلة** (chunking) لتجاوز حد السياق للسير الضخمة.
- **إعادة المحاولة (Retry):** عند فشل JSON، إعادة الطلب مع تعليمات أبسط بدل عرض النص الخام.
