# تقرير تحليلي مفصل — `AiEvaluationService.php`

> **الملف:** `app/Services/AiEvaluationService.php`
> **السطور:** 92 سطر
> **النوع:** خدمة (Service) — تقييم أداء الموظفين بالذكاء الاصطناعي
> **التبعيات:** `App\Models\Employee`, `App\Services\AiService`, `OpenAI\Laravel\Facades\OpenAI`

---

## ١. الدور في النظام

`AiEvaluationService` هي خدمة متخصصة في **تقييم أداء الموظفين شغّالاً** بناءً على بيانات حقيقية مسجّلة في قاعدة البيانات. تجمع البيانات الإحصائية للموظف (المهام، الإجازات، التقارير)، تبني Prompt عربي منظّم، ترسله لنموذج الذكاء الاصطناعي، وتُرجع تقريراً شاملاً بصيغة Markdown.

```
موارد بشرية يضغط "تقييم ذكاء اصطناعي"
            │
            ▼
    ┌───────────────────────────┐
    │   AiEvaluationService     │ ← هذا الملف
    │───────────────────────────│
    │ 1. gatherEmployeeData()   │ → جمع البيانات من DB
    │ 2. buildPrompt()          │ → بناء نص عربي
    │ 3. AiService::chat()      │ → إرسال للنموذج
    │ 4. return Markdown string │ → تقرير منسّق
    └───────────────────────────┘
```

### من يستخدم هذه الخدمة؟

| المستدعي | الملف | كيفية الاستخدام |
|----------|------|----------------|
| `EmployeeResource` (HR) | `app/Filament/Hr/Resources/EmployeeResource.php` | زر "تقييم ذكاء اصطناعي" على كل موظف نشط |

```php
// في EmployeeResource.php
->modalContent(fn (Employee $record) => view(
    'filament.pages.ai-evaluation',
    ['evaluation' => app(AiEvaluationService::class)->evaluate($record)]
))
```

---

## ٢. الكود الكامل مع التحليل سطراً بسطر

### ٢.١ تعريف الكلاس

```php
namespace App\Services;

use App\Models\Employee;
use OpenAI\Laravel\Facades\OpenAI;

class AiEvaluationService
{
```

- **`use App\Models\Employee`**: يستقبل نموذج الموظف كاملاً ويستخرج بياناته عبر العلاقات.
- **`use OpenAI\Laravel\Facades\OpenAI`**: مستورد لكن **غير مستخدم فعلياً** — الخدمة تستخدم `AiService` بدلاً منه (سطر 16). هذا استيراد متبقٍ من تطور الكود.

> **ملاحظة:** الاعتماد على `AiService` (وليس `OpenAI` Facade مباشرة) هو القرار الصحيح لأنه يمر عبر وسيط LiteLLM.

---

### ٢.٢ الدالة الرئيسية: `evaluate()`

```php
public function evaluate(Employee $employee): string
{
    $data = $this->gatherEmployeeData($employee);
    $prompt = $this->buildPrompt($data);
```

**تدفق التنفيذ (3 خطوات):**
1. **جمع البيانات** — `gatherEmployeeData()` تستخرج 15 مؤشراً من قاعدة البيانات
2. **بناء الـ Prompt** — `buildPrompt()` تصيغ البيانات كنص عربي منظّم
3. **الإرسال والاستقبال** — تُرسل الرسائل للذكاء الاصطناعي وتُرجع النتيجة

```php
    try {
        $aiService = app(\App\Services\AiService::class);
```

- **حقن الخدمة عبر Service Container:** `app(AiService::class)` بدلاً من `new AiService()`. هذا يتيح:
  - حقن التبعيات المستقبلية تلقائياً
  - سهولة الاختبار (Mocking) في الاختبارات
  - إعادة استخدام نفس الكائن (Singleton إذا سُجّل كذلك)

```php
        $response = $aiService->chat([
            ['role' => 'system', 'content' => "أنت مدير موارد بشرية خبير ومتخصص في تقييم الأداء الوظيفي. قم بتقييم أداء الموظف بناءً على البيانات المقدمة. أجب باللغة العربية بتنسيق Markdown مفصل ومنظم.\n\nيجب أن تكون الاستجابة بالشكل التالي:\n\n## 📊 التقييم العام\n[تحديد مستوى الأداء: ممتاز / جيد جداً / جيد / مقبول / ضعيف مع تبرير مختصر]\n\n## 📈 تحليل الأداء المهامي\n[تحليل مفصل لأداء المهام: نسبة الإنجاز، التأخير، الكفاءة]\n\n## 💪 نقاط القوة\n[قائمة بنقاط القوة الملاحظة]\n\n## ⚠️ نقاط تحتاج لتحسين\n[قائمة بنقاط الضعف والمجالات التي تحتاج تطوير]\n\n## 📋 تحليل الحضور والإجازات\n[تقييم أنماط الحضور والإجازات]\n\n## 📝 تحليل التواصل والتقارير\n[تقييم نشاط التقارير والتواصل]\n\n## 🎯 التوصيات وخطة التحسين\n[توصيات عملية مفصلة وخطوات محددة للتحسين]\n\n## 📌 الخلاصة\n[ملخص نهائي مختصر]\n\nاستخدم العناوين والقوائم والخط العريض والتنسيقات الأخرى في Markdown لجعل التقرير واضحاً ومنظماً ومفصلاً."],
            ['role' => 'user', 'content' => $prompt],
        ]);
```

**تحليل رسالة النظام (System Message):**

رسالة النظام هي **تعليمات توجيهية** تتحكم بسلوك النموذج. تحتوي على:

| العنصر | الوصف |
|--------|------|
| **الدور** | "مدير موارد بشرية خبير ومتخصص في تقييم الأداء الوظيفي" |
| **اللغة** | "أجب باللغة العربية" |
| **التنسيق** | "بصيغة Markdown مفصل ومنظم" |
| **الهيكل الإلزامي** | 8 أقسام محددة بعناوين Emoji |

**أقسام التقرير الإلزامية الثمانية:**

| القسم | العنوان | المحتوى المطلوب |
|------|---------|----------------|
| 1 | 📊 التقييم العام | مستوى الأداء (ممتاز/جيد جداً/جيد/مقبول/ضعيف) + تبرير |
| 2 | 📈 تحليل الأداء المهامي | نسبة الإنجاز، التأخير، الكفاءة |
| 3 | 💪 نقاط القوة | قائمة بنقاط القوة الملاحظة |
| 4 | ⚠️ نقاط تحتاج لتحسين | قائمة بنقاط الضعف والمجالات التي تحتاج تطوير |
| 5 | 📋 تحليل الحضور والإجازات | تقييم أنماط الحضور والإجازات |
| 6 | 📝 تحليل التواصل والتقارير | تقييم نشاط التقارير والتواصل |
| 7 | 🎯 التوصيات وخطة التحسين | توصيات عملية مفصلة وخطوات محددة |
| 8 | 📌 الخلاصة | ملخص نهائي مختصر |

> **تقنية Prompt Engineering:** تحديد الهيكل بدقة (عناوين، أقسام، تنسيق) يجبر النموذج على إنتاج تقرير ثابت البنية لكن مرن المحتوى في كل مرة.

```php
        if ($response && isset($response['choices'][0]['message']['content'])) {
            return $response['choices'][0]['message']['content'];
        }

        return 'لم يتم تلقي استجابة صحيحة من الذكاء الاصطناعي.';
```

- **فحص الاستجابة:** يتأكد من وجود المسار `choices[0].message.content` في JSON المُرجع.
- إذا وُجد، يُرجع النص المُولّد (Markdown string).
- إذا لم يوجد (استجابة فارغة أو بنية غير متوقعة)، يُرجع رسالة خطأ بالعربية.

```php
    } catch (\Exception $e) {
        return 'خطأ في الاتصال بخدمة الذكاء الاصطناعي: ' . $e->getMessage();
    }
}
```

- **معالجة الاستثناءات:** إذا حدث خطأ غير متوقع (مثل نفاد الذاكرة)، يُرجع رسالة خطأ مع التفاصيل.

> **ملاحظة تصميمية:** هذه الدالة تُرجع `string` دائماً (وليس `null`)، مما يسهّل على الواجهة عرض النتيجة دائماً — سواء كانت تقييماً ناجحاً أو رسالة خطأ.

---

### ٢.٣ دالة جمع البيانات: `gatherEmployeeData()`

```php
protected function gatherEmployeeData(Employee $employee): array
{
    $employee->load(['tasks', 'sentReports', 'receivedReports', 'leaves', 'user']);
```

**Eager Loading (تحميل مسبق):**
- `$employee->load([...])` يحمّل جميع العلاقات المطلوبة في استعلام واحد لمنع مشكلة **N+1 Queries**.
- بدون هذا، كل استعلام `->tasks` أو `->leaves` سيُنفّذ استعلاماً منفصلاً.

**العلاقات المُحمَّلة:**

| العلاقة | النوع | الاستخدام |
|---------|------|----------|
| `tasks` | HasMany | إحصائيات المهام |
| `sentReports` | HasMany | التقارير المرسلة |
| `receivedReports` | HasMany | التقارير المستلمة (محمّلة لكن غير مستخدمة في الإحصائيات) |
| `leaves` | HasMany | إحصائيات الإجازات |
| `user` | BelongsTo | اسم الموظف |

```php
    $tasks = $employee->tasks;
    $totalTasks = $tasks->count();
    $doneTasks = $tasks->where('status', 'done')->count();
    $overdueTasks = $tasks->filter(fn ($t) =>
        $t->due_date && $t->due_date < now() && $t->status !== 'done')->count();
    $inProgressTasks = $tasks->where('status', 'in_progress')->count();
```

**إحصائيات المهام (Collection Methods):**

| المتغير | المنطق | الوصف |
|---------|--------|------|
| `$totalTasks` | `$tasks->count()` | إجمالي المهام المسندة |
| `$doneTasks` | `->where('status', 'done')->count()` | المهام المكتملة |
| `$overdueTasks` | `->filter(fn ($t) => ...)` | **المهام المتأخرة:** تاريخ الاستحقاق أصبح من الماضي والحالة ليست `done` |
| `$inProgressTasks` | `->where('status', 'in_progress')` | المهام قيد التنفيذ حالياً |

> **تحليل المنطق الدقيق للمهام المتأخرة:** `$t->due_date && $t->due_date < now() && $t->status !== 'done'`
> - الشرط الأول: `due_date` ليس null
> - الشرط الثاني: تاريخ الاستحقاق قد مرّ
> - الشرط الثالث: المهمة لم تُنجز بعد
> - مهمة بدون تاريخ استحقاق لا تُحتسب متأخرة حتى لو لم تُنجز

```php
    $totalLeaves = $employee->leaves->count();
    $approvedLeaves = $employee->leaves->where('status', 'approved_by_hr')->count();
```

**إحصائيات الإجازات:**
- `$totalLeaves`: إجمالي طلبات الإجازات (بكل حالاتها)
- `$approvedLeaves`: فقط المعتمدة نهائياً من HR (`approved_by_hr`) — وليس `approved_by_head`

```php
    $sentReports = $employee->sentReports->count();
    $repliedReports = $employee->sentReports->where('status', 'replied')->count();
```

**إحصائيات التواصل:**
- `$sentReports`: عدد التقارير التي أرسلها الموظف
- `$repliedReports`: عدد التقارير التي تم الرد عليها (`status = replied`) — مقياس لفاعلية التواصل

```php
    return [
        'name' => $employee->user?->name,
        'job_title' => $employee->job_title,
        'department' => $employee->department?->name,
        'status' => $employee->status,
        'salary' => $employee->salary,
        'hire_date' => $employee->hire_date?->format('Y-m-d'),
        'total_tasks' => $totalTasks,
        'done_tasks' => $doneTasks,
        'overdue_tasks' => $overdueTasks,
        'in_progress_tasks' => $inProgressTasks,
        'task_completion_rate' => $totalTasks > 0
            ? round(($doneTasks / $totalTasks) * 100, 1) : 0,
        'total_leaves' => $totalLeaves,
        'approved_leaves' => $approvedLeaves,
        'sent_reports' => $sentReports,
        'replied_reports' => $repliedReports,
    ];
}
```

**البيانات الـ 15 المُجمَّعة:**

| # | المفتاح | المصدر | الوصف |
|---|---------|--------|------|
| 1 | `name` | `$employee->user?->name` | اسم الموظف (`?->` للأمان) |
| 2 | `job_title` | `$employee->job_title` | المسمى الوظيفي |
| 3 | `department` | `$employee->department?->name` | القسم |
| 4 | `status` | `$employee->status` | الحالة الوظيفية |
| 5 | `salary` | `$employee->salary` | الراتب |
| 6 | `hire_date` | `$employee->hire_date?->format('Y-m-d')` | تاريخ التعيين بصيغة ISO |
| 7 | `total_tasks` | محسوب | إجمالي المهام |
| 8 | `done_tasks` | محسوب | المهام المنجزة |
| 9 | `overdue_tasks` | محسوب | المهام المتأخرة |
| 10 | `in_progress_tasks` | محسوب | المهام قيد التنفيذ |
| 11 | `task_completion_rate` | محسوب | **نسبة الإنجاز %** = (منجزة / إجمالي) × 100 |
| 12 | `total_leaves` | محسوب | إجمالي الإجازات |
| 13 | `approved_leaves` | محسوب | الإجازات المعتمدة |
| 14 | `sent_reports` | محسوب | التقارير المرسلة |
| 15 | `replied_reports` | محسوب | التقارير المُردودة |

> **حساب نسبة الإنجاز:** `$totalTasks > 0 ? round(($doneTasks / $totalTasks) * 100, 1) : 0`
> - التحقق من القسمة على صفر (موظف بدون مهام → 0%)
> - التقريب لمنزلة عشرية واحدة (مثل 85.7%)

---

### ٢.٤ دالة بناء الـ Prompt: `buildPrompt()`

```php
protected function buildPrompt(array $data): string
{
    return "قم بتقييم أداء الموظف التالي:\n\n"
        . "الاسم: {$data['name']}\n"
        . "المسمى الوظيفي: {$data['job_title']}\n"
        . "القسم: " . ($data['department'] ?? 'غير محدد') . "\n"
        . "الحالة: {$data['status']}\n"
        . "تاريخ التعيين: {$data['hire_date']}\n\n"
        . "إحصائيات الأداء:\n"
        . "- إجمالي المهام: {$data['total_tasks']}\n"
        . "- المهام المنتهية: {$data['done_tasks']}\n"
        . "- المهام المتأخرة: {$data['overdue_tasks']}\n"
        . "- المهام قيد التنفيذ: {$data['in_progress_tasks']}\n"
        . "- نسبة الإنجاز: {$data['task_completion_rate']}%\n"
        . "- إجمالي الإجازات: {$data['total_leaves']}\n"
        . "- الإجازات المعتمدة: {$data['approved_leaves']}\n"
        . "- التقارير المرسلة: {$data['sent_reports']}\n"
        . "- التقارير التي تم الرد عليها: {$data['replied_reports']}\n\n"
        . "قم بتقديم:\n"
        . "1. تقييم عام للأداء (ممتاز/جيد جداً/جيد/مقبول/ضعيف)\n"
        . "2. نقاط القوة\n"
        . "3. نقاط تحتاج لتحسين\n"
        . "4. توصيات عملية";
}
```

**تحليل بنية الـ Prompt:**

الـ Prompt مقسّم إلى 4 أقسام:

```
┌─────────────────────────────────────────────┐
│ 1. تعليمة opening                           │
│    "قم بتقييم أداء الموظف التالي:"          │
├─────────────────────────────────────────────┤
│ 2. البيانات الأساسية (5 حقول)               │
│    الاسم، المسمى، القسم، الحالة، التعيين    │
├─────────────────────────────────────────────┤
│ 3. إحصائيات الأداء (10 مؤشرات)              │
│    المهام (5)، الإجازات (2)، التقارير (2)،  │
│    نسبة الإنجاز (1)                          │
├─────────────────────────────────────────────┤
│ 4. تعليمات الإخراج (4 طلبات)                │
│    تقييم عام، نقاط قوة، نقاط ضعف، توصيات    │
└─────────────────────────────────────────────┘
```

> **مثال على Prompt مُولّد:**
> ```
> قم بتقييم أداء الموظف التالي:
>
> الاسم: رنا قدور
> المسمى الوظيفي: Senior Laravel Developer
> القسم: تطوير الويب
> الحالة: active
> تاريخ التعيين: 2024-01-15
>
> إحصائيات الأداء:
> - إجمالي المهام: 24
> - المهام المنتهية: 18
> - المهام المتأخرة: 2
> - المهام قيد التنفيذ: 4
> - نسبة الإنجاز: 75%
> - إجمالي الإجازات: 3
> - الإجازات المعتمدة: 2
> - التقارير المرسلة: 12
> - التقارير التي تم الرد عليها: 9
>
> قم بتقديم:
> 1. تقييم عام للأداء (ممتاز/جيد جداً/جيد/مقبول/ضعيف)
> 2. نقاط القوة
> 3. نقاط تحتاج لتحسين
> 4. توصيات عملية
> ```

> **معالجة القيمة الفارغة:** `($data['department'] ?? 'غير محدد')` — إذا لم يكن للموظف قسم، يعرض "غير محدد" بدلاً من null.

---

## ٣. مخطط تدفق المنطق الكامل

```
        ┌─────────────────────┐
        │  Employee $record   │
        │  (من قاعدة البيانات) │
        └──────────┬──────────┘
                   │
                   ▼
    ┌──────────────────────────────┐
    │   gatherEmployeeData()       │
    │──────────────────────────────│
    │ Step 1: Eager Load           │
    │   $employee->load([          │
    │     'tasks','sentReports',   │
    │     'receivedReports',       │
    │     'leaves','user'          │
    │   ])                         │
    │                              │
    │ Step 2: Compute Stats        │
    │   • total/done/overdue tasks │
    │   • task_completion_rate %   │
    │   • total/approved leaves    │
    │   • sent/replied reports     │
    │                              │
    │ Step 3: Return Array (15)    │
    └──────────────┬───────────────┘
                   │
                   ▼
    ┌──────────────────────────────┐
    │   buildPrompt()              │
    │──────────────────────────────│
    │ تحويل المصفوفة إلى نص عربي   │
    │ منظّم بفقرات وقوائم          │
    └──────────────┬───────────────┘
                   │
                   ▼
    ┌──────────────────────────────┐
    │   AiService::chat()          │
    │──────────────────────────────│
    │ System: خبير HR + 8 أقسام   │
    │ User:   البيانات + الطلب     │
    └──────────────┬───────────────┘
                   │
                   ▼
    ┌──────────────────────────────┐
    │   فحص الاستجابة              │
    │──────────────────────────────│
    │ $response['choices'][0]      │
    │   ['message']['content']     │
    └──────────────┬───────────────┘
                   │
          ┌────────┴────────┐
          │                 │
      نجاح │                 │ فشل
          │                 │
          ▼                 ▼
   ┌────────────┐  ┌─────────────────────┐
   │ Markdown   │  │ "لم يتم تلقي..."    │
   │ string     │  │ أو رسالة خطأ        │
   └────────────┘  └─────────────────────┘
```

---

## ٤. أنماط التصميم المستخدمة

| النمط | الموقع | الوصف |
|------|--------|------|
| **Service Layer** | الكلاس بأكمله | منطق أعمال معقد منفصل عن وحدات التحكم |
| **Template Method** | `evaluate()` → `gatherEmployeeData()` → `buildPrompt()` | تدفق ثابت من الخطوات |
| **Dependency Injection** | `app(AiService::class)` | حقن الخدمة عبر Service Container |
| **Data Transfer Object (DTO)** | المصفوفة المُرجعة من `gatherEmployeeData()` | نقل بيانات منظّمة بين الدوال |
| **Null-safe Operator** | `$employee->user?->name` | حماية من null في السلاسل |

---

## ٥. خصائص التقييم

### ٥.١ تقييم توليدي (Generative)

| الخاصية | القيمة | الأثر |
|---------|--------|------|
| **عدم الحفظ** | التقييم لا يُخزّن في قاعدة البيانات | يُعاد توليده في كل مرة |
| **العشوائية المحكومة** | `temperature = 0.7` | تقييمات متشابهة لكن ليست متطابقة |
| **اللغة** | عربية دائماً | موجهة للـ HR العربي |
| **التنسيق** | Markdown | قابل للعرض كـ HTML منسّق |

### ٥.٢ جودة التقييم تعتمد على البيانات

```
موظف ببيانات غنية (50 مهمة، 20 تقرير) → تقييم دقيق ومفيد
موظف ببيانات فقيرة (0 مهام، 0 تقارير) → تقييم غير ذي معنى
```

> هذا أحد القيود الأساسية للنظام — موظف جديد بدون بيانات سيحصل على تقييم عام لا يعكس أداءه الحقيقي.

---

## ٦. العرض في الواجهة

التقييم يُعرض في **نافذة منبثقة (Modal)** كبيرة في صفحة الموظف:

```php
// في EmployeeResource.php
Tables\Actions\Action::make('ai_evaluate')
    ->label(__('filament.actions.ai_evaluate'))
    ->modalHeading(__('filament.actions.ai_evaluate_heading'))
    ->modalContent(fn (Employee $record) => view(
        'filament.pages.ai-evaluation',
        ['evaluation' => app(AiEvaluationService::class)->evaluate($record)]
    ))
```

**قالب العرض** (`filament/pages/ai-evaluation.blade.php`):

```blade
{!! Illuminate\Support\Str::markdown($evaluation) !!}
```

يحوّل Markdown إلى HTML منسّق مع دعم كامل للعناوين والقوائم والخط العريض.

### قيود عرض زر التقييم:

```php
->visible(fn (Employee $record) =>
    $record->status === 'active'
    && auth()->id() !== $record->user_id
    && auth()->user()?->role?->name !== 'super_admin'
)
```

| الشرط | السبب |
|-------|------|
| `$record->status === 'active'` | فقط الموظفين النشطين |
| `auth()->id() !== $record->user_id` | لا يقيم نفسه |
| ليس `super_admin` | المدير العام لا يقيم (يتركها لـ HR) |

---

## ٧. نقاط القوة والقيود

### نقاط القوة

| النقطة | الوصف |
|--------|------|
| **بيانات حقيقية** | التقييم مبني على أرقام فعلية من قاعدة البيانات، لا آراء ذاتية |
| **تقرير شامل** | 8 أقسام تغطي الأداء من زوايا متعددة |
| **Prompt هندسي** | هيكل إلزامي يضمن جودة وتناسق المخرجات |
| **Eager Loading** | استعلام واحد لكل البيانات (أداء ممتاز) |
| **معالجة أخطاء** | لا يتعطل النظام أبداً — يُرجع رسالة دائماً |

### القيود

| القيد | الوصف |
|------|------|
| **عدم الحفظ** | التقييم يُعاد توليده في كل مرة (يستهلك وقت و tokens) |
| **اعتماد على جودة البيانات** | موظف ببيانات قليلة → تقييم ضعيف |
| **زمن الاستجابة** | قد يستغرق 10-60 ثانية للتوليد |
| **تكلفة API** | كل تقييم يستهلك tokens من حصة الذكاء الاصطناعي |
| **عشوائية** | قد يختلف التقييم قليلاً بين استدعاءين (temperature 0.7) |

---

*نهاية تقرير `AiEvaluationService.php`*
